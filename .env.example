# Reference values for local production-style testing or Cloud Run env vars.
# Do not put real secrets in source control.

APP_ENV=production
SECRET_KEY=replace-with-a-generated-secret-key
STAFF_PASSWORD=replace-with-a-strong-staff-password
MAX_TICKETS=30

# Flask uses this locally; Cloud Run provides PORT automatically.
PORT=5000
